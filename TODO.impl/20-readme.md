Status: DONE — implemented and verified this session.

# 20 — README

Priority: 3

## Deliverable

`README.md`: what it is (TS port of Fontist, npm + CLI), install (`npm i fontist` /
`npx fontist`), quickstart API (find/install/manifest), CLI usage examples incl.
`fontist update` first-run, install locations table, license acceptance behavior,
environment variables (FONTIST_PATH etc.), relationship to the Ruby gem and the formulas
repo, development section (build/test commands).

## Acceptance

- Examples match the implemented API/CLI exactly (no aspirational docs).
